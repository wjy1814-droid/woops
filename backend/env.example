# 데이터베이스 설정 방법 1: DATABASE_URL 사용 (클라우드 배포 권장)
# DATABASE_URL=postgresql://사용자명:비밀번호@호스트:포트/데이터베이스명

# 데이터베이스 설정 방법 2: 개별 환경 변수 사용 (로컬 개발)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=memo_app
DB_USER=postgres
DB_PASSWORD=your_password

# 환경 설정
NODE_ENV=development

# 서버 포트
PORT=3000

