# ☁️ 메모 공유 앱 - 구름 메모장

하늘 배경과 구름 형태의 메모를 작성할 수 있는 웹 애플리케이션입니다.

## ✨ 주요 기능

- 📝 메모 작성, 수정, 삭제 (CRUD)
- ☁️ 구름 형태의 메모 디자인
- 🌤️ 하늘 배경 테마
- 🔄 실시간 메모 공유
- 📱 반응형 디자인

## 🛠️ 기술 스택

### 프론트엔드
- HTML5
- CSS3 (구름 애니메이션 포함)
- JavaScript (Vanilla JS)

### 백엔드
- Node.js
- Express.js
- PostgreSQL

## 📋 사전 요구사항

- Node.js (v14 이상)
- PostgreSQL (v12 이상)
- npm 또는 yarn

## 🚀 설치 및 실행 방법

### 1. 저장소 클론
\`\`\`bash
git clone https://github.com/wjy1814-droid/woops.git
cd woops
\`\`\`

### 2. 데이터베이스 설정

PostgreSQL에서 데이터베이스를 생성합니다:

\`\`\`sql
CREATE DATABASE memo_app;
\`\`\`

### 3. 백엔드 설정

\`\`\`bash
cd backend

# 패키지 설치
npm install

# 환경 변수 설정
# env.example 파일을 참고하여 .env 파일을 생성합니다
cp env.example .env

# .env 파일을 열어 데이터베이스 정보를 입력합니다
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=memo_app
# DB_USER=postgres
# DB_PASSWORD=your_password
# PORT=3000
\`\`\`

### 4. 백엔드 서버 실행

\`\`\`bash
# 개발 모드 (nodemon 사용)
npm run dev

# 또는 일반 모드
npm start
\`\`\`

서버가 `http://localhost:3000`에서 실행됩니다.

### 5. 프론트엔드 실행

새 터미널을 열어서:

\`\`\`bash
cd frontend

# 간단한 HTTP 서버 실행 (여러 방법 중 선택)

# Python 3가 설치된 경우:
python -m http.server 8000

# Python 2가 설치된 경우:
python -m SimpleHTTPServer 8000

# Node.js http-server가 설치된 경우:
npx http-server -p 8000

# VS Code의 Live Server 확장을 사용하는 경우:
# index.html 파일에서 우클릭 > "Open with Live Server"
\`\`\`

브라우저에서 `http://localhost:8000`으로 접속합니다.

## 📁 프로젝트 구조

\`\`\`
woops/
├── frontend/
│   ├── index.html      # 메인 HTML 파일
│   ├── style.css       # 스타일시트 (구름 디자인)
│   └── app.js          # JavaScript 로직
├── backend/
│   ├── server.js       # Express 서버
│   ├── database.js     # PostgreSQL 연결
│   ├── routes/
│   │   └── memos.js    # 메모 API 라우트
│   ├── package.json    # 백엔드 의존성
│   └── env.example     # 환경 변수 예시
├── docs/
│   └── 메모 공유앱.md  # 프로젝트 문서
├── .gitignore
└── README.md
\`\`\`

## 🔌 API 엔드포인트

| 메서드 | 엔드포인트 | 설명 |
|--------|------------|------|
| GET | `/api/memos` | 모든 메모 조회 |
| GET | `/api/memos/:id` | 특정 메모 조회 |
| POST | `/api/memos` | 새 메모 생성 |
| PUT | `/api/memos/:id` | 메모 수정 |
| DELETE | `/api/memos/:id` | 메모 삭제 |

## 💡 사용 방법

1. 상단의 입력 칸에 메모를 작성합니다
2. "메모 추가" 버튼을 클릭하거나 Enter 키를 누릅니다
3. 메모가 구름 형태로 표시됩니다
4. 각 메모의 "수정" 버튼으로 내용을 변경할 수 있습니다
5. "삭제" 버튼으로 메모를 제거할 수 있습니다

## 🎨 디자인 특징

- **하늘 배경**: 그라데이션으로 표현된 맑은 하늘
- **구름 메모**: 부드러운 곡선의 구름 형태 카드
- **떠다니는 애니메이션**: 메모가 하늘에 떠있는 듯한 효과
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 모두 지원

## 🐛 문제 해결

### 데이터베이스 연결 오류
- PostgreSQL이 실행 중인지 확인하세요
- `.env` 파일의 데이터베이스 정보가 올바른지 확인하세요

### 백엔드 서버 오류
- 포트 3000이 이미 사용 중인 경우 `.env`에서 다른 포트를 지정하세요
- 필요한 npm 패키지가 모두 설치되었는지 확인하세요

### 프론트엔드 연결 오류
- 백엔드 서버가 실행 중인지 확인하세요
- CORS 오류가 발생하면 백엔드의 CORS 설정을 확인하세요

## 📝 라이선스

MIT License

## 👤 제작자

학습 목적으로 제작된 프로젝트입니다.

